# Production Web to Production API.
# Loaded by Vite when `npm run build` runs (mode=production).
# This URL is public. Do not put secrets here.

VITE_API_BASE_URL=https://api.acomi.in/api/v1
VITE_API_TIMEOUT_MS=30000
VITE_APP_ENV=production

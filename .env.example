# CountIn Web — copy to `.env` and adjust as needed.
# Use relative `/api/v1` in development so Vite proxies to Spring Boot (avoids CORS).

VITE_API_BASE_URL=/api/v1
VITE_API_TIMEOUT_MS=30000
VITE_APP_ENV=development

/** Elevation tokens — soft card shadow aligned with mobile. */
export const elevation = {
  sm: '0 2px 10px rgba(16, 24, 40, 0.05)',
  md: '0 4px 14px rgba(16, 24, 40, 0.08)',
  lg: '0 8px 20px rgba(16, 24, 40, 0.10)',
} as const;

export const LAYOUT = {
  /** Hover-expanded rail. In-flow reservation stays at `sidebarCollapsedWidth`. */
  sidebarWidth: 256,
  sidebarCollapsedWidth: 72,
  sidebarTransitionMs: 200,
  headerHeight: 64,
  contentMaxWidth: 1600,
} as const;
